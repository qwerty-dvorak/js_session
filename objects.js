let person = {
    name: "Abhinav",
    age: 23,
    city: "Delhi"
};

console.log(person.name);
person.age = 24;      // Update value
console.log(person);

const obj = { x: 10, y: 20 };
const { x, y } = obj;
console.log(x, y); 

